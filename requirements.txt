# Pinned to versions verified to compile and run end-to-end on Python 3.12,
# CPU-only (no CUDA, no MPS), 31 GB RAM. See SETUP.md for installation.

# PII detection (HuggingFace token classifier)
transformers>=5.6.0
torch>=2.4.0
accelerate>=1.0.0
safetensors>=0.4.0
tokenizers>=0.21.0

# Dataset loaders
datasets>=3.0.0
huggingface-hub>=0.27.0

# Synthetic-PII generator
faker>=26.0.0

# Downstream NER
spacy>=3.8.0

# Numerics
numpy>=1.26.0
packaging>=23.0
